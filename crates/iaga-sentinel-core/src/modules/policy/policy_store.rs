// Legacy hardcoded policy store — replaced by storage::traits::PolicyStore + storage::sqlite::SqliteStorage.
// This module is kept empty for backwards compatibility. All policy lookups now go through AppState.
