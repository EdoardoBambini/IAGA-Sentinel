# Generated by IAGA Sentinel (`iaga-codex export-rules`) — DO NOT EDIT BY HAND.
# Source APL bundle SHA-256: 4d464c7a22623d1f6fbb79af06d1c9de3ce83bae07dd800cb96bba9f45f961e4
# Static execpolicy layer: 2 prefix rule(s) compiled, 3 policy(ies) runtime-only.
# The hash above makes drift between this file and the APL bundle detectable.
# Syntax validated against `codex execpolicy check`; see ADR 0022 and the integration README for the pinned Codex version.

prefix_rule(
    pattern = ["curl"],
    decision = "forbidden",
    justification = "IAGA Sentinel: no_external_egress. use the approved proxy, not direct curl",
    match = [["curl"]],
    not_match = [["true"]],
)

prefix_rule(
    pattern = ["rm", "-rf"],
    decision = "prompt",
    justification = "IAGA Sentinel: confirm_destructive_rm. destructive recursive delete needs confirmation",
    match = [["rm", "-rf"]],
    not_match = [["true"]],
)

# Runtime-only policies (no static command-prefix equivalent; enforced by the
# `iaga-codex hook` gate against POST /v1/inspect, not by this file):
#   - halt_high_risk_shell: `>` comparison has no command-prefix equivalent
#   - no_pipe_to_shell: call `contains(...)` has no command-prefix equivalent
#   - default_allow: catch-all `when true` has no command prefix
