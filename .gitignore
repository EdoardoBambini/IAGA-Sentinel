target/
node_modules/
dist/
.env
.env.*
*.key
*.pem
*.db
*.sqlite
.DS_Store
.claude/
npm-debug.log*
Thumbs.db

# Local development files
.agents/
skills-lock.json

# Benchmark scripts/data (charts/ PNGs are committed for README)
benchmark_v2.py
benchmark_results.csv
benchmark_v2_results.csv
*.pyc
__pycache__/

# Internal docs
docs/PRD.md
IAGA_SENTINEL_1.0.md
IAGA_SENTINEL_1.1.md
IAGA_SENTINEL_1.2.md
IAGA_SENTINEL_1.3.md

# Research paper artifacts (kept local, never in public repo)
docs/aisec-2026/

# Internal Enterprise roadmap & design docs (NEVER push to public repo).
# Contain product strategy, milestone plans, and ADRs for the
# IAGA Sentinel Enterprise commercial product.
ROADMAP_ENTERPRISE.md
ENTERPRISE_*.md
INTERNAL_*.md
docs/internal/
docs/enterprise/

# Confidential strategy artifacts that sometimes land in the working dir.
# Roadmap and GTM PDFs (in any folder), and the untracked FEATURES draft,
# stay local only.
*.pdf
FEATURES.md
IAGA_SENTINEL_FEATURES_AND_POSITIONING.md

# Stale or internal docs pulled from the public repo (kept local on disk).
docs/ROADMAP_v0.4.0.md
docs/DEMO.md

# Media (large, kept local). Committed README assets are GIF/PNG
# (media/*.gif, charts/*.png); raw video stays local in any folder.
media/hero.mp4
*.mp4
*.mov

# 1.0 M1: build artifacts + dev leftovers
*.log
!.claude/**/*.log
# Local-only Remotion promo video project (NEVER push to public repo).
# Whole folder is ignored: source, node_modules, and rendered out/.
iaga-sentinel-video/
crates/iaga-sentinel-core/*.db
crates/iaga-sentinel-core/*.sqlite
